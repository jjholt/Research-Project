\section{Visualisations}
\begin{figure}\centering
    \incfig[]{ITAP}
    \caption{}
    \label{fig:ITAP}
\end{figure}
\begin{enumerate}
    \item Forces experienced at the \textbf{flange} \& at the \textbf{stem}
    \item Strain  parallel to ITAP (Elastic modulus for titanium 6/4 is 127 GPa, so possibly infinitessimal strain theory?)
    \item 
\end{enumerate}
\begin{enumerate}
    \item Stem has varying lengths depending on length of bone remaining after amputation.
    \item We could pick a couple of different lengths or have the assumption that after finding the dampen ratio, we can describe all sizes.
    \item Characteristics of the medium change?
\end{enumerate}

\begin{figure}\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel = Length of prosthetic, ylabel = Amplitude, title = Generate wave forms,
        domain=0:40,
        samples=100,
        % axis lines = left, ymax=1.2, ymin=-1.2
        ]
        \addplot[] {cos(deg(x))*exp(-0.1*x)};
    \end{axis}
\end{tikzpicture}
\end{figure}
\begin{figure}\centering
    \begin{tikzpicture}
        \begin{axis}[
            xlabel = Length of prosthetic, ylabel = Amplitude, title = Define damping ratio,
            domain=0:40,
            samples=100,
            % axis lines = left, ymax=1.2, ymin=-1.2
            ]
            \addplot[] {cos(deg(x))*exp(-0.1*x)};
            \addplot[dashed] {exp(-0.1*x)};
        \end{axis}
\end{tikzpicture}
\begin{tikzpicture}
    \begin{axis}[
        xlabel = Length of prosthetic, ylabel = Amplitude, title = (?) Determine threshold of perception,
        domain=0:40,
        samples=100,
        % axis lines = left, ymax=1.2, ymin=-1.2
        ]
        \addplot[] {cos(deg(x))*exp(-0.1*x)};
        \addplot[dashed, red, domain=25:40] {0.02};
        \draw[-stealth] (axis cs: 35,0.5) node[above]{Perception}-- (axis cs: 35,0.03);
    \end{axis}
\end{tikzpicture}
\end{figure}
